{
  "name": "@tt/types",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "yarn clean",
    "build": "tsc",
    "prestart": "yarn clean",
    "start": "tsc --watch",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@tt/utils": "1.0.0"
  },
  "devDependencies": {
    "typescript": "~4.0.3",
    "rimraf": "^3.0.2"
  }
}
