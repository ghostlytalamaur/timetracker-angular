{
  "name": "@tt/types",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "tsc --watch --preserveWatchOutput",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write **/*.{ts,html,js,json}",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@tt/utils": "1.0.0"
  },
  "devDependencies": {
    "typescript": "~4.0.3",
    "rimraf": "^3.0.2"
  }
}
