{
  "name": "timetracker",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "start": "concurrently -r -c \"blue.bold,magenta.bold,green.bold,red.bold\" -k -n \"types,utils,backend,frontend\" \"yarn workspace @tt/types start\" \"yarn workspace @tt/utils start\" \"yarn workspace @tt/backend start\" \"yarn workspace @tt/frontend start\"",
    "lint": "yarn workspaces run lint",
    "format": "yarn workspaces run format",
    "clean": "yarn workspaces run clean",
    "build:prepare": "yarn ngcc && yarn workspace @tt/utils build && yarn workspace @tt/types build",
    "prepare": "husky install"
  },
  "lint-staged": {
    "**/*.{ts,js,html,scss,css,json}": "prettier --write"
  },
  "devDependencies": {
    "typescript": "~4.0.3",
    "prettier": "2.2.1",
    "eslint": "7.10.0",
    "@typescript-eslint/eslint-plugin": "4.3.0",
    "@typescript-eslint/parser": "4.3.0",
    "concurrently": "^6.0.2",
    "husky": "^6.0.0",
    "lint-staged": "^10.5.4"
  }
}
